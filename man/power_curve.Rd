% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_null.R
\name{power_curve}
\alias{power_curve}
\alias{print.power_curve}
\title{Detectable-effect (power) curve for an incremental genomic signature}
\usage{
power_curve(
  data,
  predictors,
  clinical,
  time = "time",
  event = "event",
  effect_sizes = c(0, 0.1, 0.2, 0.35, 0.5),
  n_sim = 20L,
  power_target = 0.8,
  n_folds = 5L,
  n_repeats = 5L,
  alpha = 0.5,
  seed = 1L
)
}
\arguments{
\item{data, predictors, clinical, time, event}{As in \code{\link[=assess_null]{assess_null()}}. \code{clinical}
is required (its fitted linear predictor is the retained baseline).}

\item{effect_sizes}{Numeric vector of injected effect sizes on the
hazard-log scale (default \code{c(0, 0.1, 0.2, 0.35, 0.5)}). \code{0} estimates the
false-positive rate.}

\item{n_sim}{Simulations per effect size (default 20).}

\item{power_target}{Target detection rate for the minimum detectable effect
(default 0.8).}

\item{n_folds, n_repeats, alpha, seed}{Passed to \code{\link[=cv_validate_survival]{cv_validate_survival()}}.}
}
\value{
An object of class \code{power_curve}: a list with \code{curve} (a data frame:
\code{effect}, \code{mean_delta}, \code{detect_rate}), \code{mde}, \code{mde_delta},
\code{power_target}, and \code{meta}. Has a \code{print} method and \code{\link[=plot_power_curve]{plot_power_curve()}}.
}
\description{
A \code{dC ~ 0} result is only interpretable once you know what \code{dC} the cohort
could have detected in the first place. This runs a \strong{semi-synthetic power
analysis}: it preserves the real clinical linear predictor, sample size and
event rate, then injects a single synthetic prognostic feature of known
strength into the outcome \emph{and} into the predictor block, and measures how
often the \code{\link[=cv_validate_survival]{cv_validate_survival()}} harness recovers a positive gain. Sweeping
the injected effect size traces the harness's power curve and pins down the
\strong{minimum detectable effect} at a target power.
}
\details{
For each \code{effect_size} and simulation, a standardized signal \code{s} is drawn;
survival times are regenerated from the hazard
\code{exp(clinical_lp + effect_size * s)} (so the clinical signal is retained and
\code{s} is the only genomic signal present), \code{s} is appended to \code{predictors}, and
the harness is run. Detection is \code{dC} lower bound \verb{> 0}. The reported
\code{mde} is the smallest injected effect whose detection rate reaches
\code{power_target}, with \code{mde_delta} its mean recovered \code{dC}.
}
\examples{
\donttest{
set.seed(1)
n <- 200
age <- rnorm(n, 60, 10)
g <- matrix(rnorm(n * 15), n, dimnames = list(NULL, paste0("g", 1:15)))
d <- data.frame(time = rexp(n, exp(0.04 * (age - 60))),
  event = rbinom(n, 1, 0.7), age = age, g)
power_curve(d, predictors = paste0("g", 1:15), clinical = "age",
  effect_sizes = c(0, 0.3, 0.6), n_sim = 5, n_repeats = 5)
}
}
\seealso{
\code{\link[=assess_null]{assess_null()}}, \code{\link[=learning_curve]{learning_curve()}}
}
