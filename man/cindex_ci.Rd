% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_survival.R
\name{cindex_ci}
\alias{cindex_ci}
\title{Bootstrap confidence interval for Harrell's C-index}
\usage{
cindex_ci(time, event, risk, n_boot = 1000L, conf = 0.95, seed = 1L)
}
\arguments{
\item{time}{Numeric follow-up time per subject.}

\item{event}{Event indicator (1/\code{TRUE} = event, 0/\code{FALSE} = censored).}

\item{risk}{Numeric risk score, higher = higher hazard / shorter survival.}

\item{n_boot}{Number of bootstrap resamples (default 1000).}

\item{conf}{Confidence level (default 0.95).}

\item{seed}{Random seed (default 1).}
}
\value{
A named numeric vector \code{c(cindex, lower, upper)}.
}
\description{
The concordance (C) index with a non-parametric percentile bootstrap
interval. \code{risk} is a per-subject risk score where \strong{higher = worse
survival} (e.g. a Cox linear predictor); the interval is obtained by
resampling subjects with replacement and recomputing C.
}
\examples{
set.seed(1)
risk <- rnorm(200)
time <- rexp(200, rate = exp(0.8 * risk))
event <- rbinom(200, 1, 0.7)
cindex_ci(time, event, risk, n_boot = 200)
}
