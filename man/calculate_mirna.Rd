% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_mirna.R
\name{calculate_mirna}
\alias{calculate_mirna}
\title{Calculate miRNA interaction gene rankings}
\usage{
calculate_mirna(mirna_matrix, normalize = TRUE)
}
\arguments{
\item{mirna_matrix}{A numeric matrix or data frame with genes as rows
and miRNAs as columns, where values represent interaction counts.
Typically produced by querying TargetScan for cancer-associated miRNAs.}

\item{normalize}{Logical; if \code{TRUE} (default), scores are normalized to
sum to 1.}
}
\value{
A named numeric vector of miRNA interaction scores sorted in
decreasing order. If \code{normalize = TRUE}, values sum to 1.
}
\description{
Builds a gene ranking based on the number of cancer-associated miRNA
interactions per gene. Uses a pre-computed interaction matrix (genes x
miRNAs) where each cell counts the number of targeting interactions.
}
\examples{
mat <- matrix(c(3, 0, 1, 2, 1, 0), nrow = 3,
  dimnames = list(c("TP53", "KRAS", "BRAF"), c("miR-1", "miR-2")))
calculate_mirna(mat)
}
