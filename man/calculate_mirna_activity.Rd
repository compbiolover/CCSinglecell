% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_mirna_activity.R
\name{calculate_mirna_activity}
\alias{calculate_mirna_activity}
\title{Calculate expression-anchored miRNA activity gene rankings}
\usage{
calculate_mirna_activity(
  mirna_expr,
  gene_expr,
  target_matrix,
  method = c("anticorrelation", "signed"),
  normalize = TRUE
)
}
\arguments{
\item{mirna_expr}{Numeric matrix of miRNA expression (miRNAs as rows,
samples as columns). Must have row and column names.}

\item{gene_expr}{Numeric matrix of gene expression (genes as rows, samples
as columns). Must have row and column names. Samples are matched to
\code{mirna_expr} by column name.}

\item{target_matrix}{Predicted gene-miRNA interactions (genes as rows,
miRNAs as columns), e.g. from TargetScan for cancer-associated miRNAs.
Non-zero entries mark candidate interactions; their magnitude is used as a
weight. This is the same kind of matrix consumed by \code{\link[=calculate_mirna]{calculate_mirna()}}.}

\item{method}{Either \code{"anticorrelation"} (default; only repressive, i.e.
negative, correlations contribute evidence) or \code{"signed"} (positive
correlations subtract, capturing net regulatory direction).}

\item{normalize}{Logical; if \code{TRUE} (default), scores are divided by the sum
of their absolute values. For \code{method = "anticorrelation"} scores are
non-negative and therefore sum to 1; for \code{method = "signed"} scores may be
negative, so they are put on a comparable scale but are not guaranteed to
sum to 1.}
}
\value{
A named numeric vector of miRNA-activity scores sorted in decreasing
order. When \code{normalize = TRUE}, non-negative (\code{"anticorrelation"}) scores
sum to 1; signed scores are scaled by the sum of absolute values and may be
negative.
}
\description{
An expression-anchored alternative to \code{\link[=calculate_mirna]{calculate_mirna()}}. Instead of
counting how many cancer miRNAs are \emph{predicted} to target a gene, this ranks
genes by how strongly they are \emph{observed} to be repressed by their targeting
miRNAs in the user's own samples. For each predicted gene-miRNA interaction,
the score accumulates the anti-correlation between the gene's and the
miRNA's expression across matched samples (the classic miRNA-repression /
competing-endogenous-RNA signal). Genes whose targeting miRNAs actually move
opposite to them score highest.
}
\examples{
set.seed(1)
samples <- paste0("s", 1:20)
genes <- paste0("g", 1:5)
mirs <- paste0("mir", 1:3)
gene_expr <- matrix(rnorm(5 * 20), nrow = 5, dimnames = list(genes, samples))
mirna_expr <- matrix(rnorm(3 * 20), nrow = 3, dimnames = list(mirs, samples))
# Make g1 strongly anti-correlated with mir1
mirna_expr["mir1", ] <- -gene_expr["g1", ] + rnorm(20, sd = 0.1)
target_matrix <- matrix(1, nrow = 5, ncol = 3, dimnames = list(genes, mirs))
calculate_mirna_activity(mirna_expr, gene_expr, target_matrix)
}
