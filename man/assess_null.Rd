% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_null.R
\name{assess_null}
\alias{assess_null}
\alias{print.null_assessment}
\title{Is a null genomic gain distinguishable from noise? (permutation test)}
\usage{
assess_null(
  data,
  predictors,
  clinical,
  time = "time",
  event = "event",
  n_perm = 200L,
  n_folds = 5L,
  n_repeats = 10L,
  alpha = 0.5,
  seed = 1L
)
}
\arguments{
\item{data}{Data frame: one row per subject with \code{time}, \code{event},
\code{predictors}, and \code{clinical} columns.}

\item{predictors}{Character vector of genomic feature columns (the block
whose incremental value is being tested).}

\item{clinical}{Character vector of clinical covariate columns (the baseline
held fixed under the null). Required.}

\item{time, event}{Column names of follow-up time and 0/1 event indicator.}

\item{n_perm}{Number of predictor-block permutations (default 200).}

\item{n_folds, n_repeats, alpha, seed}{Passed to \code{\link[=cv_validate_survival]{cv_validate_survival()}}.
\code{n_repeats} is used only for the reported full-strength interval; each
permutation uses a single fixed-fold pass for speed and comparability.}
}
\value{
An object of class \code{null_assessment}: a list with \code{observed} (the
single-pass gain), \code{observed_ci} (the full \code{n_repeats} gain with interval
and win-rate, from \code{\link[=cv_validate_survival]{cv_validate_survival()}}), \code{null} (the vector of
permuted gains), \code{p_value}, and \code{meta}. Has a \code{print} method and
\code{\link[=plot_null_assessment]{plot_null_assessment()}}.
}
\description{
\code{\link[=cv_validate_survival]{cv_validate_survival()}} can report that adding a genomic signature to the
clinical baseline yields \code{dC ~ 0}. This function decides whether that gain is
any larger than what a \emph{randomly aligned} genomic block of the same size and
correlation structure would produce — the difference between "no signal" and
"signal too small to matter here."
}
\details{
The observed statistic is the out-of-fold \code{combined - clinical} C-index gain
from a single event-stratified penalized-CV pass (the same engine as
\code{\link[=cv_validate_survival]{cv_validate_survival()}}). The null distribution is built by \strong{permuting only
the rows of the genomic predictor block} \code{n_perm} times — this breaks the
genomics ↔ outcome link while leaving the clinical ↔ outcome signal (and the
fold assignment) intact, so the test isolates the \emph{increment} genomics claims
to add. The one-sided empirical p-value is
\verb{(1 + #\{null >= observed\}) / (1 + n_perm)}.
}
\examples{
\donttest{
set.seed(1)
n <- 200
g <- matrix(rnorm(n * 15), n, dimnames = list(NULL, paste0("g", 1:15)))
age <- rnorm(n, 60, 10)
# genes are pure noise; only age drives survival
d <- data.frame(time = rexp(n, exp(0.04 * (age - 60))),
  event = rbinom(n, 1, 0.7), age = age, g)
assess_null(d, predictors = paste0("g", 1:15), clinical = "age",
  n_perm = 50, n_repeats = 5)
}
}
\seealso{
\code{\link[=power_curve]{power_curve()}}, \code{\link[=learning_curve]{learning_curve()}}, \code{\link[=cv_validate_survival]{cv_validate_survival()}}
}
