% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denoise_magic.R
\name{denoise_magic}
\alias{denoise_magic}
\title{Denoise single-cell RNA-seq data with MAGIC}
\usage{
denoise_magic(
  expression_matrix,
  seed = 123L,
  knn = 10L,
  solver = "approximate",
  n_jobs = 1L
)
}
\arguments{
\item{expression_matrix}{Numeric matrix with genes as rows and cells as
columns.}

\item{seed}{Integer random seed for reproducibility (default 123).}

\item{knn}{Integer number of nearest neighbours (default 10).}

\item{solver}{Character; \code{"approximate"} (default) or \code{"exact"}.}

\item{n_jobs}{Integer number of parallel jobs (default 1).}
}
\value{
A list with two elements:
\describe{
\item{denoised}{Numeric matrix (genes x cells) of denoised values.}
\item{gene_metadata}{Data frame with \code{gene_short_name} column, suitable
for Monocle3 cell data set construction.}
}
}
\description{
Applies the MAGIC (Markov Affinity-based Graph Imputation of Cells)
algorithm to denoise a single-cell expression matrix.
}
\examples{
\dontrun{
result <- denoise_magic(raw_counts)
denoised_mat <- result$denoised
}
}
