% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_km_curve.R
\name{plot_km_curve}
\alias{plot_km_curve}
\title{Plot a Kaplan-Meier survival curve for risk groups}
\usage{
plot_km_curve(
  risk_df,
  title = NULL,
  pval = TRUE,
  conf_int = TRUE,
  risk_table = TRUE,
  palette = c("#E7B800", "#2E9FDF")
)
}
\arguments{
\item{risk_df}{A data frame with \code{time}, \code{vital_status}, and \code{risk_group}
columns (as returned by \code{\link[=calculate_risk_scores]{calculate_risk_scores()}}).}

\item{title}{Optional plot title.}

\item{pval}{Logical; annotate the log-rank p-value (default \code{TRUE}).}

\item{conf_int}{Logical; draw confidence bands (default \code{TRUE}).}

\item{risk_table}{Logical; add a numbers-at-risk table (default \code{TRUE}).}

\item{palette}{Colours for the high/low risk groups.}
}
\value{
A \code{ggsurvplot} object (a list containing a \pkg{ggplot2} plot).
}
\description{
Wraps \code{\link[survminer:ggsurvplot]{survminer::ggsurvplot()}} to draw the paper's primary result: how
well a risk score stratifies patients into high- and low-risk groups. This
consumes the output of \code{\link[=calculate_risk_scores]{calculate_risk_scores()}}.
}
\examples{
\dontrun{
risk <- calculate_risk_scores(patient_df, model$active_genes, model$coefficients)
plot_km_curve(risk, title = "TCGA-COAD")
}
}
