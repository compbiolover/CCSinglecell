% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_mad.R
\name{calculate_mad}
\alias{calculate_mad}
\title{Calculate Median Absolute Deviation (MAD) gene rankings}
\usage{
calculate_mad(expression_matrix, normalize = TRUE)
}
\arguments{
\item{expression_matrix}{Numeric matrix with genes as rows and cells as
columns. Must have row names (gene identifiers).}

\item{normalize}{Logical; if \code{TRUE} (default), scores are normalized to
sum to 1.}
}
\value{
A named numeric vector of MAD values sorted in decreasing order.
If \code{normalize = TRUE}, values sum to 1.
}
\description{
Ranks genes by their expression variability across cells using the median
absolute deviation. Higher MAD indicates more variable expression.
}
\examples{
mat <- matrix(rnorm(30), nrow = 3, dimnames = list(paste0("g", 1:3), NULL))
calculate_mad(mat)
}
