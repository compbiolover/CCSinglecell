% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cox_model.R
\name{fit_cox_model}
\alias{fit_cox_model}
\title{Fit a penalized Cox proportional hazards model}
\usage{
fit_cox_model(
  cox_df,
  gene_names,
  alpha = 1,
  nfolds = 10L,
  seed = 1L,
  max_genes = NULL
)
}
\arguments{
\item{cox_df}{Data frame containing gene expression columns plus
\code{days_to_last_follow_up} (numeric) and \code{vital_status} (0/1).}

\item{gene_names}{Character vector of gene column names to use as
predictors. Genes absent from \code{cox_df} are silently dropped.}

\item{alpha}{Elastic-net mixing parameter: 1 = lasso (default),
0 = ridge.}

\item{nfolds}{Integer number of cross-validation folds (default 10).}

\item{seed}{Integer random seed for reproducibility (default 1).}

\item{max_genes}{Maximum number of genes to include (default \code{NULL},
uses all).}
}
\value{
A list with:
\describe{
\item{cv_fit}{The \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}} object.}
\item{active_genes}{Character vector of genes with non-zero
coefficients.}
\item{coefficients}{Named numeric vector of non-zero coefficients.}
}
}
\description{
Selects survival-predictive genes using elastic-net regularized Cox
regression via \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.
}
\examples{
\dontrun{
result <- fit_cox_model(patient_data, gene_names = top_genes)
result$active_genes
}
}
