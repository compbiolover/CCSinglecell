% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate_mofa.R
\name{integrate_mofa}
\alias{integrate_mofa}
\title{Learn per-gene attributions with MOFA (unsupervised latent factors)}
\usage{
integrate_mofa(blocks, nfactors = 5, normalize = TRUE, use_basilisk = FALSE, ...)
}
\arguments{
\item{blocks}{A \strong{named} list of numeric matrices, one per omics layer, with
\strong{samples as rows} and features (genes / miRNAs) as columns. Every block
must have the same samples in the same order (matched by row name). (This is
the same orientation \code{\link[=integrate_diablo]{integrate_diablo()}} takes; MOFA's own
features-by-samples layout is handled internally.)}

\item{nfactors}{Number of latent factors to fit (default 5). MOFA prunes
factors that explain no variance, so the final attribution may pool over
fewer.}

\item{normalize}{Logical; if \code{TRUE} (default) the returned attributions are
divided by their sum so they sum to 1.}

\item{use_basilisk}{Passed to \code{\link[MOFA2:run_mofa]{MOFA2::run_mofa()}}; if \code{TRUE}, MOFA2 manages
its own Python environment via \code{basilisk} instead of the active
\code{reticulate} binding. Default \code{FALSE}.}

\item{...}{Further arguments merged into the MOFA \strong{model} options (e.g.
\code{likelihoods}, \code{spikeslab_weights}); see \code{\link[MOFA2:get_default_model_options]{MOFA2::get_default_model_options()}}.}
}
\value{
A named numeric vector of per-feature attributions (mean absolute
factor weight across factors, pooled across views), sorted decreasing and
summing to 1 when \code{normalize = TRUE}. The trained model is attached as
attribute \code{"model"} and the raw per-view weights as \code{"weights"}. The result
is a ready-to-use ranking: feed it to \code{\link[=score_rankings]{score_rankings()}} as one metric, or
use it directly as a learned, unsupervised integration.
}
\description{
The unsupervised, latent-factor counterpart to \code{\link[=integrate_diablo]{integrate_diablo()}}. Where
DIABLO learns against a response, \code{integrate_mofa()} needs no outcome: it fits
MOFA (\code{MOFA2::run_mofa}), an unsupervised multi-omics factor model, and turns
the learned per-view feature weights into a single per-feature attribution
ranking. Genes (and miRNAs) that load strongly on the shared latent factors —
the axes of coordinated variation across omics layers — score highest.
}
\details{
This is the MOFA backend from \code{ROADMAP.md} (Phase 3b). It is gated behind the
optional \code{MOFA2} package, which in turn drives a Python \code{mofapy2} backend via
\code{reticulate}. If no Python binding is configured, the function looks for a
virtualenv named \code{r-mofapy2} (created with \code{mofapy2} installed); otherwise it
respects \code{RETICULATE_PYTHON} / any Python you have already initialised, or set
\code{use_basilisk = TRUE} to let MOFA2 manage its own environment.
}
\examples{
\donttest{
if (requireNamespace("MOFA2", quietly = TRUE)) {
  set.seed(1)
  n <- 40
  rna <- matrix(rnorm(n * 8), n, dimnames = list(paste0("s", 1:n), paste0("g", 1:8)))
  mir <- matrix(rnorm(n * 5), n, dimnames = list(paste0("s", 1:n), paste0("mir", 1:5)))
  # a shared latent axis that couples g1 and mir1
  z <- rnorm(n)
  rna[, "g1"] <- 3 * z + rnorm(n, 0, 0.3)
  mir[, "mir1"] <- -3 * z + rnorm(n, 0, 0.3)
  attr_scores <- integrate_mofa(list(rna = rna, mir = mir))
  head(attr_scores)
}
}
}
