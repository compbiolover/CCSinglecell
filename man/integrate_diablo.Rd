% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate_diablo.R
\name{integrate_diablo}
\alias{integrate_diablo}
\title{Learn per-gene attributions with DIABLO (supervised multi-block)}
\usage{
integrate_diablo(blocks, response, ncomp = 2, keepX = NULL, normalize = TRUE, ...)
}
\arguments{
\item{blocks}{A \strong{named} list of numeric matrices, one per omics layer, with
\strong{samples as rows} and features (genes / miRNAs) as columns. Every block
must have the same samples in the same order (matched by row name).}

\item{response}{A per-sample class label (factor or coercible vector), length
equal to the number of rows in each block. For a survival use case, pass a
binarised outcome (e.g. high- vs low-risk).}

\item{ncomp}{Number of DIABLO components to fit (default 2). Attributions
aggregate the absolute loadings across all components.}

\item{keepX}{Optional per-block feature-selection budget passed to
\code{\link[mixOmics:block.splsda]{mixOmics::block.splsda()}} — a named list (matching \code{blocks}) of integer
vectors of length \code{ncomp}. \code{NULL} (default) keeps all features.}

\item{normalize}{Logical; if \code{TRUE} (default) the returned attributions are
divided by their sum so they sum to 1.}

\item{...}{Further arguments forwarded to \code{\link[mixOmics:block.splsda]{mixOmics::block.splsda()}} (e.g. a
custom \code{design}).}
}
\value{
A named numeric vector of per-feature attributions (mean absolute
loading across components, pooled across blocks), sorted decreasing and
summing to 1 when \code{normalize = TRUE}. The fitted model is attached as
attribute \code{"model"} and the raw per-block loadings as \code{"loadings"}. The
result is a ready-to-use ranking: feed it to \code{\link[=score_rankings]{score_rankings()}} as one
metric, or use it directly as a learned, outcome-aware integration.
}
\description{
The heavyweight, supervised counterpart to \code{\link[=learn_weights]{learn_weights()}}. Where
\code{learn_weights()} blends already-computed per-metric rankings without an
outcome, \code{integrate_diablo()} learns directly from the raw omics blocks
\emph{against a response}: it fits DIABLO (\code{mixOmics::block.splsda}), a sparse
multi-block PLS discriminant model, and turns the learned block loadings into
a single per-feature attribution ranking. Genes (and miRNAs) that the model
relies on to separate the response classes score highest.
}
\details{
This is the DIABLO backend planned in \code{ROADMAP.md} (Phase 3b). It is gated
behind the optional \code{mixOmics} dependency. The unsupervised latent-factor
backend (MOFA2) is not included: its Python/reticulate backend is not
installable in a plain R environment, so it cannot be exercised end-to-end;
add it the same way once \code{MOFA2} is available.
}
\examples{
\donttest{
if (requireNamespace("mixOmics", quietly = TRUE)) {
  set.seed(1)
  n <- 30
  rna <- matrix(rnorm(n * 8), n, dimnames = list(paste0("s", 1:n), paste0("g", 1:8)))
  mir <- matrix(rnorm(n * 5), n, dimnames = list(paste0("s", 1:n), paste0("mir", 1:5)))
  grp <- factor(rep(c("low", "high"), each = n / 2))
  # make g1 and mir1 discriminate the groups
  rna[, "g1"] <- rna[, "g1"] + as.integer(grp) * 2
  mir[, "mir1"] <- mir[, "mir1"] - as.integer(grp) * 2
  attr_scores <- integrate_diablo(list(rna = rna, mir = mir), grp)
  head(attr_scores)
}
}
}
