% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_multiomics.R
\name{score_rankings}
\alias{score_rankings}
\title{Score a gene set from an arbitrary set of gene rankings}
\usage{
score_rankings(genes, rankings, weights = NULL, renormalize = TRUE)
}
\arguments{
\item{genes}{A character vector of gene symbols, or a path to a gene file
(see \code{\link[=read_gene_list]{read_gene_list()}}).}

\item{rankings}{A \strong{named} list of named numeric vectors. Each element is
one metric's genome-wide ranking; the list name becomes the metric's
column name in the output.}

\item{weights}{Optional metric weights. Either a numeric vector (one per
element of \code{rankings}, in list order; rescaled to sum to 1), the string
\code{"learn"} to learn data-driven weights with \code{\link[=learn_weights]{learn_weights()}}, or \code{NULL}
(default) for equal weights.}

\item{renormalize}{Logical; if \code{TRUE} (default) each metric is renormalised
to sum to 1 across the requested gene set before combining.}
}
\value{
A data frame with one row per gene, sorted by \code{combined}
(decreasing): \code{gene}, one column per metric, \code{combined}, and \code{rank}. The
weights used and metric names are attached as attributes \code{"weights"} and
\code{"metrics"}.
}
\description{
The generic engine behind \code{\link[=score_gene_set]{score_gene_set()}} and \code{\link[=score_multiomics]{score_multiomics()}}.
Given a named list of per-metric gene rankings (one per omics layer /
metric), it restricts each to the requested genes, optionally renormalizes
within the gene set, and blends them into a single combined score with
\code{\link[=combine_rankings]{combine_rankings()}}.
}
\examples{
r1 <- c(g1 = 0.5, g2 = 0.3, g3 = 0.2)
r2 <- c(g2 = 0.6, g3 = 0.3, g4 = 0.1)
score_rankings(c("g1", "g2", "g3"), list(expression = r1, methylation = r2))
}
