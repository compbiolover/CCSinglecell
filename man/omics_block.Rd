% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_multiomics.R
\name{omics_block}
\alias{omics_block}
\title{Describe one omics layer for multi-omics scoring}
\usage{
omics_block(name, data, metric)
}
\arguments{
\item{name}{Character label for the layer (becomes the metric's column name
in the scored output).}

\item{data}{The omics data the metric consumes (e.g. an expression matrix,
a miRNA interaction matrix, or a list of inputs for metrics that need
several — such as \code{list(expression = ..., pseudotime = ...)}).}

\item{metric}{Either a built-in metric name (\code{"mad"}, \code{"mirna"}, or
\code{"switchde"}) or a function taking \code{data} and returning a \strong{named numeric
vector} of gene scores (higher = more important).}
}
\value{
An \code{omics_block} object.
}
\description{
Bundles an omics data object with a \emph{metric} that turns it into a
genome-wide, per-gene ranking. Pass a list of these to \code{\link[=score_multiomics]{score_multiomics()}}.
This is the extension point that lets any omics layer participate in scoring
without changing the integration core.
}
\examples{
set.seed(1)
expr <- matrix(rpois(200, 3), nrow = 20,
  dimnames = list(paste0("g", 1:20), paste0("c", 1:10)))
blk <- omics_block("expression", expr, metric = "mad")
# A custom metric: rank genes by mean expression
blk2 <- omics_block("mean_expr", expr, metric = function(m) sort(rowMeans(m), decreasing = TRUE))
}
