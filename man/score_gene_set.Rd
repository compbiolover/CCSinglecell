% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_gene_set.R
\name{score_gene_set}
\alias{score_gene_set}
\title{Score an arbitrary set of genes}
\usage{
score_gene_set(
  genes,
  expression_matrix,
  pseudotime = NULL,
  mirna_matrix = NULL,
  weights = NULL,
  renormalize = TRUE
)
}
\arguments{
\item{genes}{A character vector of gene symbols, or a path to a gene file
(see \code{\link[=read_gene_list]{read_gene_list()}}).}

\item{expression_matrix}{Numeric matrix with genes as rows and cells as
columns. Must have row names (gene identifiers).}

\item{pseudotime}{Optional numeric vector of pseudotime values, one per
cell (length \code{ncol(expression_matrix)}). Enables the \code{switchde} metric.}

\item{mirna_matrix}{Optional numeric matrix/data frame (genes x miRNAs) of
interaction counts. Enables the \code{mirna} metric.}

\item{weights}{Optional numeric vector of metric weights, one per available
metric, in the order (mad, switchde, mirna) for the metrics that are
present. Defaults to equal weights. Rescaled to sum to 1.}

\item{renormalize}{Logical; if \code{TRUE} (default) each metric's scores are
renormalised to sum to 1 \emph{across the requested gene set} before
combining, so the combined score is a proportion within the set.}
}
\value{
A data frame with one row per requested gene, sorted by \code{combined}
score (decreasing), containing:
\describe{
\item{gene}{Gene symbol.}
\item{one column per available metric}{\code{mad}, and optionally
\code{switchde} / \code{mirna} — the (renormalised) per-metric score.}
\item{combined}{Weighted combination of the available metrics.}
\item{rank}{Integer rank (1 = highest combined score).}
}
The \code{weights} used and the metric names are attached as attributes
\code{"weights"} and \code{"metrics"}.
}
\description{
High-level wrapper that scores a user-supplied set of genes using one or
more of the package's ranking metrics and combines them into a single
score. This is the main entry point for the command-line tool and for
interactive "score my genes" workflows.
}
\details{
Each metric is computed across the full \code{expression_matrix} (so scores
reflect a genome-wide ranking), then restricted to the requested \code{genes}.
The available metrics are:

\describe{
\item{\code{mad}}{Expression variability via \code{\link[=calculate_mad]{calculate_mad()}}. Always
computed.}
\item{\code{switchde}}{Switch-like dynamics along a trajectory via
\code{\link[=calculate_switchde]{calculate_switchde()}}. Computed only when \code{pseudotime} is supplied
and the \pkg{switchde} package is available.}
\item{\code{mirna}}{Cancer-miRNA targeting via \code{\link[=calculate_mirna]{calculate_mirna()}}. Computed
only when \code{mirna_matrix} is supplied.}
}

Present metrics are blended with \code{\link[=combine_rankings]{combine_rankings()}} using \code{weights}
(equal weights by default). When only one metric is available the combined
score equals that metric.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(200), nrow = 20,
  dimnames = list(paste0("g", 1:20), paste0("c", 1:10)))
score_gene_set(c("g1", "g5", "g10"), mat)
}
