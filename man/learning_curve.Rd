% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_null.R
\name{learning_curve}
\alias{learning_curve}
\alias{print.learning_curve}
\title{Learning curve: is a null power-limited or signal-limited?}
\usage{
learning_curve(
  data,
  predictors,
  clinical,
  time = "time",
  event = "event",
  fractions = c(0.4, 0.55, 0.7, 0.85, 1),
  n_subsample = 10L,
  n_folds = 5L,
  alpha = 0.5,
  seed = 1L
)
}
\arguments{
\item{data, predictors, clinical, time, event}{As in \code{\link[=assess_null]{assess_null()}}.}

\item{fractions}{Numeric training-size fractions to evaluate (default
\code{c(0.4, 0.55, 0.7, 0.85, 1.0)}).}

\item{n_subsample}{Subsamples per fraction (default 10).}

\item{n_folds, alpha, seed}{Passed to \code{\link[=cv_validate_survival]{cv_validate_survival()}} (each pass uses
\code{n_repeats = 1}).}
}
\value{
An object of class \code{learning_curve}: a list with \code{curve} (mean
C-index per model per fraction), \code{raw} (per-subsample rows),
\code{combined_slope} (mean combined C-index per subject added), and \code{meta}. Has
a \code{print} method and \code{\link[=plot_learning_curve]{plot_learning_curve()}}.
}
\description{
Traces out-of-fold C-index against training-set size for the \code{signature},
\code{clinical}, and \code{combined} models. If the \code{combined} curve is still climbing
at the full sample size, the cohort is \strong{power-limited} — more data could yet
reveal a genomic gain. If it has plateaued at (or below) the clinical curve,
the null is \strong{signal-limited} — more of the same data will not help. This is
usually the single most convincing figure that a \code{dC ~ 0} result is real.
}
\details{
Each \code{fraction} is subsampled \code{n_subsample} times (event-stratified), one
single-pass \code{\link[=cv_validate_survival]{cv_validate_survival()}} is run per subsample, and the per-model
C-indices are averaged. A least-squares slope of the mean \code{combined} C-index
against training size summarizes the trend.
}
\examples{
\donttest{
set.seed(1)
n <- 300
age <- rnorm(n, 60, 10)
g <- matrix(rnorm(n * 15), n, dimnames = list(NULL, paste0("g", 1:15)))
d <- data.frame(time = rexp(n, exp(0.04 * (age - 60))),
  event = rbinom(n, 1, 0.7), age = age, g)
learning_curve(d, predictors = paste0("g", 1:15), clinical = "age",
  fractions = c(0.5, 0.75, 1.0), n_subsample = 3)
}
}
\seealso{
\code{\link[=assess_null]{assess_null()}}, \code{\link[=power_curve]{power_curve()}}
}
