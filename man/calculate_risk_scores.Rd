% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_risk_scores.R
\name{calculate_risk_scores}
\alias{calculate_risk_scores}
\title{Calculate patient risk scores from Cox model coefficients}
\usage{
calculate_risk_scores(cox_df, active_genes, coefficients)
}
\arguments{
\item{cox_df}{Data frame with gene expression columns plus
\code{days_to_last_follow_up} and \code{vital_status}.}

\item{active_genes}{Character vector of gene names selected by the Cox
model.}

\item{coefficients}{Named numeric vector of Cox model coefficients
(same order / names as \code{active_genes}).}
}
\value{
A data frame with columns:
\describe{
\item{risk_score}{Numeric sum of binarised expression x coefficient
sign.}
\item{risk_group}{\code{"high"} or \code{"low"} relative to the median absolute
risk.}
\item{vital_status}{From \code{cox_df}.}
\item{time}{Survival time from \code{cox_df}.}
}
}
\description{
Binarises gene expression relative to per-gene medians, weights by
coefficient sign, and sums across genes to produce a risk score per
patient. Patients are then stratified into "high" and "low" risk groups.
}
\examples{
\dontrun{
scores <- calculate_risk_scores(patient_data, model$active_genes, model$coefficients)
table(scores$risk_group)
}
}
