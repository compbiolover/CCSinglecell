% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_survival.R
\name{validate_survival}
\alias{validate_survival}
\alias{print.survival_validation}
\title{Rigorously validate a survival signature}
\usage{
validate_survival(data, predictors, time = "time", event = "event",
  clinical = NULL, test = NULL, horizon = NULL, n_groups = 4L,
  n_boot = 1000L, seed = 1L)
}
\arguments{
\item{data}{Training data frame: one row per subject, with \code{time}, \code{event},
the \code{predictors}, and any \code{clinical} columns.}

\item{predictors}{Character vector of column names forming the signature to
validate (e.g. genes surfaced by \code{\link[=integrate_diablo]{integrate_diablo()}} / \code{\link[=integrate_mofa]{integrate_mofa()}}).}

\item{time, event}{Column names of the follow-up time and 0/1 event indicator.}

\item{clinical}{Optional character vector of clinical covariate columns used
for the baseline model (e.g. age, stage). Strongly recommended — a signature
that cannot beat clinical variables alone has not earned its complexity.}

\item{test}{Optional external/held-out data frame with the same columns; when
supplied, models are fit on \code{data} and evaluated on \code{test} (the honest
setting). When \code{NULL}, evaluation is in-sample and optimistic.}

\item{horizon}{Optional time point for a calibration table (same units as
\code{time}); \code{NULL} skips calibration.}

\item{n_groups}{Number of risk groups for calibration (default 4).}

\item{n_boot, seed}{Passed to \code{\link[=cindex_ci]{cindex_ci()}}.}
}
\value{
An object of class \code{survival_validation}: a list with \code{cindex} (a data
frame, one row per model — \code{signature}, \code{clinical}, \code{combined} — with
\code{cindex}/\code{lower}/\code{upper}), \code{calibration} (a data frame or \code{NULL}), and
\code{meta}. Has \code{print} and \code{\link[=plot_calibration]{plot_calibration()}} methods.
}
\description{
Evaluates a candidate gene / feature signature the way Phase 4 of the roadmap
asks for: a bootstrapped C-index, \strong{always compared against a clinical-only
baseline} (and the two combined), with optional train → external-cohort
validation and a calibration table at a chosen time horizon. Models are plain
Cox proportional-hazards fits (\code{survival::coxph}); the signature's linear
predictor is the risk score.
}
\examples{
\donttest{
set.seed(1)
n <- 300
g1 <- rnorm(n); g2 <- rnorm(n); age <- rnorm(n, 60, 10)
lp <- 0.7 * g1 - 0.5 * g2 + 0.02 * age
d <- data.frame(
  time = rexp(n, rate = exp(lp - mean(lp))),
  event = rbinom(n, 1, 0.7), g1 = g1, g2 = g2, age = age
)
validate_survival(d, predictors = c("g1", "g2"), clinical = "age",
  horizon = 1, n_boot = 200)
}
}
