% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_multiomics.R
\name{score_multiomics}
\alias{score_multiomics}
\title{Score a gene set across an arbitrary number of omics layers}
\usage{
score_multiomics(genes, blocks, weights = NULL, renormalize = TRUE)
}
\arguments{
\item{genes}{A character vector of gene symbols, or a path to a gene file
(see \code{\link[=read_gene_list]{read_gene_list()}}).}

\item{blocks}{A list of \code{\link[=omics_block]{omics_block()}} objects (one per omics layer).}

\item{weights}{Optional numeric vector of per-block weights, in block
order. Defaults to equal weights.}

\item{renormalize}{Logical; passed to \code{\link[=score_rankings]{score_rankings()}}.}
}
\value{
A scored data frame, identical in shape to \code{\link[=score_gene_set]{score_gene_set()}} but
with one column per block.
}
\description{
Truly multi-omics scoring: evaluate each \code{\link[=omics_block]{omics_block()}} to a genome-wide
gene ranking, then blend them into one combined score. Unlike
\code{\link[=score_gene_set]{score_gene_set()}} (which is limited to the built-in expression / pseudotime
/ miRNA metrics), this accepts any number of layers with any metrics.
}
\examples{
set.seed(1)
expr <- matrix(rpois(400, 3), nrow = 40,
  dimnames = list(paste0("g", 1:40), paste0("c", 1:10)))
mir <- matrix(sample(0:3, 120, replace = TRUE), nrow = 40,
  dimnames = list(paste0("g", 1:40), paste0("mir", 1:3)))
meth <- matrix(runif(400), nrow = 40,
  dimnames = list(paste0("g", 1:40), paste0("c", 1:10)))
blocks <- list(
  omics_block("expression", expr, "mad"),
  omics_block("mirna", mir, "mirna"),
  omics_block("methylation", meth, function(m) sort(apply(m, 1, stats::sd), decreasing = TRUE))
)
score_multiomics(paste0("g", 1:40), blocks)
}
