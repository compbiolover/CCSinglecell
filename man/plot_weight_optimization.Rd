% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_weight_optimization.R
\name{plot_weight_optimization}
\alias{plot_weight_optimization}
\title{Plot metric-weight optimisation results}
\usage{
plot_weight_optimization(
  results,
  metric_col = "cindex",
  title = "Weight optimisation"
)
}
\arguments{
\item{results}{A data frame with weight columns (\code{w1}, \code{w2}, and optionally
\code{w3}) and a performance column (default name \code{cindex}). This can be built
by evaluating each row of an \code{\link[=optimize_weights]{optimize_weights()}} grid.}

\item{metric_col}{Name of the performance column (default \code{"cindex"}).}

\item{title}{Optional plot title.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Visualises how model performance (e.g. concordance / C-index) varies across
the grid of metric weights explored during optimisation. For two metrics a
line chart of performance versus the first weight is drawn; for three
metrics a \code{w1} x \code{w2} heatmap is drawn (the third weight is
\code{1 - w1 - w2}).
}
\examples{
grid <- data.frame(w1 = seq(0, 1, 0.1), w2 = seq(1, 0, -0.1))
grid$cindex <- 0.5 + 0.2 * grid$w1 - 0.15 * grid$w1^2
plot_weight_optimization(grid)
}
